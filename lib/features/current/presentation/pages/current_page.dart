import 'package:flutter/material.dart';

class CurrentPage extends StatelessWidget{

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("you are already logged"),
    );
  }
}